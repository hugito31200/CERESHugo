attackGoal(performAttack(_)).
isScada(hvac).

hacl(internet, hvac, ipSec, _).

hacl(hvac, hospitalnetwork, _, _).
isNetwork(hospitalnetwork).

hacl(hvac, hospitalnetwork, _, _).
hasAccount(alarm_provider,hvac,root).
inCompetent(alarm_provider).
malicious(alarm_provider).
inCompetent(technician).
malicious(technician).
physicalAccess(technician,chu).
attackerLocated(internet).